---
interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={['container', className]} {...rest}>
  <slot />
</div>

<style>
  .container {
    max-width: 1000px;
    margin-inline: auto;
  }
</style>
